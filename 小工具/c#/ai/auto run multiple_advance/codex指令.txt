讀取AGENTS.md
加一個按鈕在刪除進程旁邊，新增一個新form
監控CPU使用率並可以設定開啟process CPU親和性，CPU priority
盡量不要動態產生

----------------------------------------------------------

按 套用到目前進程 時，會先把這個 item 的 CPU 設定寫回 setting，再套用到正在執行的 process
不要只儲存設定按鈕，CPU 設定改完就寫回對應item的設定 
關閉程式才存參數

----------------------------------------------------------

選取item 要更新CPU設定form參數

----------------------------------------------------------

----------------------------------------------------------

----------------------------------------------------------